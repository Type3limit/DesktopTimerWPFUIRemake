<ui:FluentWindow
    x:Class="DesktopTimer.Views.Window.Conversation.DeepSeekConversation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:con="clr-namespace:DesktopTimer.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DesktopTimer.Views.Window.Conversation"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:DesktopTimer.Models.DeepSeek"
    xmlns:tool="comm"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DeepSeekConversation"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance Type={x:Type model:DeepSeek}}"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Views/Styles/ImageSource.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Header="DeepSeekAI" />


        <ui:ListView Grid.Row="1" ItemsSource="{Binding CurrentConversations, UpdateSourceTrigger=PropertyChanged}">
            <ui:ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ui:ListView.ItemsPanel>

            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="3*" />
                        </Grid.ColumnDefinitions>
                    </Grid>


                </DataTemplate>
            </ui:ListView.ItemTemplate>

        </ui:ListView>

        <GridSplitter
            Grid.Row="1"
            Height="1"
            VerticalAlignment="Bottom"
            ResizeDirection="Rows" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <ui:TextBox
                Grid.Column="0"
                PreviewKeyDown="TextBox_PreviewKeyDown"
                Text="{Binding CurrentInputingContent.Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ui:Button
                Grid.Column="1"
                Command="{Binding StartCompletionsCommand}"
                Icon="{ui:SymbolIcon Symbol=ArrowUp48}" />
        </Grid>

    </Grid>
</ui:FluentWindow>
